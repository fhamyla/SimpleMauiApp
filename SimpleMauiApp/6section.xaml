<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SimpleMauiApp._6section">
    <ScrollView>
        <VerticalStackLayout>
            <!-- Chapter Title -->
            <Label 
                Text="Specialized Operating Systems and Their Use Cases"
                FontSize="24"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Margin="10" />

            <!-- Introduction -->
            <Label 
                Text="Operating Systems come in various types, each designed to address specific use cases. This chapter explores these types and their applications."
                FontSize="16"
                Margin="10"
                HorizontalTextAlignment="Center" />

            <!-- Single-User OS -->
            <Label 
                Text="1. Single-User Operating System"
                FontSize="18"
                FontAttributes="Bold"
                Margin="10" />
            <Label 
                Text="A Single-User OS is designed for one user at a time, managing resources and applications for that individual. It is commonly used in personal computers and mobile devices."
                FontSize="16"
                Margin="10"
                HorizontalTextAlignment="Start" />
            <Label 
                Text="Example: Windows 10, macOS, Android."
                FontSize="14"
                Margin="10"
                HorizontalTextAlignment="Start" />

            <!-- Multi-User OS -->
            <Label 
                Text="2. Multi-User Operating System"
                FontSize="18"
                FontAttributes="Bold"
                Margin="10" />
            <Label 
                Text="A Multi-User OS allows multiple users to access a computer system simultaneously. It ensures fair resource allocation and user security."
                FontSize="16"
                Margin="10"
                HorizontalTextAlignment="Start" />
            <Label 
                Text="Example: UNIX, mainframe systems."
                FontSize="14"
                Margin="10"
                HorizontalTextAlignment="Start" />

            <!-- Real-Time OS -->
            <Label 
                Text="3. Real-Time Operating System (RTOS)"
                FontSize="18"
                FontAttributes="Bold"
                Margin="10" />
            <Label 
                Text="An RTOS is designed to process data and respond within strict time constraints, making it ideal for embedded systems and critical applications."
                FontSize="16"
                Margin="10"
                HorizontalTextAlignment="Start" />
            <Label 
                Text="Example: VxWorks, FreeRTOS, QNX."
                FontSize="14"
                Margin="10"
                HorizontalTextAlignment="Start" />

            <!-- Distributed OS -->
            <Label 
                Text="4. Distributed Operating System"
                FontSize="18"
                FontAttributes="Bold"
                Margin="10" />
            <Label 
                Text="A Distributed OS manages a group of independent computers and presents them as a single cohesive system to the user. It is widely used in cloud and networked environments."
                FontSize="16"
                Margin="10"
                HorizontalTextAlignment="Start" />
            <Label 
                Text="Example: Google’s Chrome OS, Microsoft Azure."
                FontSize="14"
                Margin="10"
                HorizontalTextAlignment="Start" />

            <!-- Embedded OS -->
            <Label 
                Text="5. Embedded Operating System"
                FontSize="18"
                FontAttributes="Bold"
                Margin="10" />
            <Label 
                Text="Embedded OSs are tailored for specialized devices with specific functionality, such as IoT devices and consumer electronics. They are lightweight and optimized for their purpose."
                FontSize="16"
                Margin="10"
                HorizontalTextAlignment="Start" />
            <Label 
                Text="Example: Embedded Linux, Windows IoT Core."
                FontSize="14"
                Margin="10"
                HorizontalTextAlignment="Start" />

            <!-- Network OS -->
            <Label 
                Text="6. Network Operating System"
                FontSize="18"
                FontAttributes="Bold"
                Margin="10" />
            <Label 
                Text="A Network OS facilitates communication and resource sharing among computers on a network. It provides security, scalability, and centralized management."
                FontSize="16"
                Margin="10"
                HorizontalTextAlignment="Start" />
            <Label 
                Text="Example: Windows Server, Novell NetWare."
                FontSize="14"
                Margin="10"
                HorizontalTextAlignment="Start" />

            <!-- Mobile OS -->
            <Label 
                Text="7. Mobile Operating System"
                FontSize="18"
                FontAttributes="Bold"
                Margin="10" />
            <Label 
                Text="Mobile OSs are designed for smartphones, tablets, and other portable devices, emphasizing touch interfaces, connectivity, and power management."
                FontSize="16"
                Margin="10"
                HorizontalTextAlignment="Start" />
            <Label 
                Text="Example: Android, iOS."
                FontSize="14"
                Margin="10"
                HorizontalTextAlignment="Start" />

            <!-- Summary -->
            <Label 
                Text="Each type of Operating System addresses unique requirements, contributing to the diverse technological ecosystem we interact with daily."
                FontSize="16"
                Margin="10" 
                HorizontalTextAlignment="Center" />

            <!-- Next Button -->
            <Button
                Text="Continue"
                BackgroundColor="Blue"
                TextColor="White"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                VerticalOptions="End"
                Margin="10"
                Clicked="NextClicked" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>